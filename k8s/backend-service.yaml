apiVersion: v1
kind: Service
metadata:
  name: parivar-census-backend
  namespace: parivar-census
spec:
  selector:
    app: parivar-census-backend

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080

  type: NodePort